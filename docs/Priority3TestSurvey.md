# Priority 3 Test Survey

Status: active planning artifact (non-executable)

This document is a first-pass survey for Priority 3: Test Suite Signal Quality.

It is not an approved implementation checklist and does not authorize test changes by itself.

Each table is organized by test file.

- Column 1 records the current test copy.
- Column 2 records the current decision (`Keep`, `Replace`, `Remove`, or `Investigate`).
- Column 3 captures notes when a decision needs explanation or follow-up.

## src/tests/backend/MongodbService.test.ts

| Test Copy | Decision | Notes |
| --- | --- | --- |
| POST to auth with unknown credentials should return error 404 | Keep | |
| POST to auth with incorrect password should 401 | Keep | |
| POST to auth with user id and correct password should return auth token | Replace | Compares only the first 70 characters of a time-sensitive token generated by the same helper used in production. Replace with a decoded-claim assertion that checks the returned token contract directly. |
| POST to auth with email and correct password should return auth token | Replace | Same issue as the user-id auth token test: partial string comparison against a time-sensitive token is brittle and under-specified. |
| POST to auth with hardcoded credentials should return auth token | Keep | Special-case moderator login is real product behavior; the assertion is minimal but still covers an important contract seam. |
| POST to /user with id should return user with same id and 201 User created | Keep | |
| POST to /user without id should return 400: Invalid user id 'undefined' | Keep | |
| POST to /user with guest id and admin credentials should fail | Keep | |
| POST to /user with guest id and same credentials should succeed | Keep | |
| POST to /user without credentials should create user | Keep | Conditional on repo policy, but still meaningful contract coverage for the public-create path. |
| POST to /user with admin-only properties without credentials should error 403 | Keep | |
| POST to /user with admin-only properties without admin credentials should error 403 | Keep | |
| POST to /user without admin credentials should fail | Replace | Meaningful auth coverage, but this title is duplicated for two different cases. Replace with clearer, case-specific test names so the suite explains 401 vs 403 outcomes. |
| POST to /user without admin credentials should fail | Replace | Meaningful auth coverage, but this title is duplicated for two different cases. Replace with clearer, case-specific test names so the suite explains 401 vs 403 outcomes. |
| POST to /user with a guestUserId as targetId should fail | Keep | |
| POST to /user with existing username should return 409 user exists | Replace | This currently depends on the earlier successful create-user test having already inserted `testNewUser`. Replace with a self-contained duplicate-user setup so order does not carry the signal. |
| GET to /auth with newly created user should return authtoken | Replace | Depends on earlier user creation and uses the same brittle token-prefix comparison as the other auth token tests. |
| GET to /user/{userId} where userId does not exist should return 404 | Investigate | The test title says the target user is unknown, but the expected body is `Authenticating user is unknown`, which suggests a stale assumption or mismatched setup. |
| GET to /user/{userId} should return User and 200 | Keep | |
| GET to /user should return list of users | Keep | |
| GET to /user with admin credentials can return list of users with email | Keep | |
| GET get to /user/{userId} should return user | Remove | Duplicates the earlier admin `userGET` success path without adding a materially different contract check. |
| GET to /user/{userId} with public user | Keep | |
| PUT to /user/{userId} with no credentials should return 401 | Keep | |
| PUT to /user/{userId} with improper credentials should return 403 | Keep | |
| PUT to /user/{userId} with public, own credentials cannot modify isAdmin | Keep | |
| PUT to /user/{userId} with public, own credentials cannot modify isVerified | Keep | |
| PUT to /user/{userId} with own credentials should alter user return 204 User updated | Keep | |
| PUT to /user/{userId} with very long password should not timeout | Replace | The current assertion really checks that a long password update succeeds, not that it meets a timing contract. Replace with a clearer behavior-focused test name and contract. |
| PUT to /user/{userId} with invalid name, email, password should fail | Keep | Broad, but it still exercises meaningful validation rejection on a real update path. |
| PUT to /user/{userId} with admin credentials should alter user return 204 User updated | Keep | |
| Admins should be able to modify guest users | Keep | |
| Guest users should never be made into admins | Keep | |
| PUT to /user/{userId} where userId does not exist (and admin credentials) should return 404 | Keep | |
| DELETE to /user/{userId} with no credentials should return 401 | Keep | |
| DELETE to /user/{userId} with improper credentials should return 403 | Keep | |
| DELETE to /user/{userId} where userId does not exist and admin credentials should return 404 | Keep | |
| DELETE to /user/{userId} with hardcoded admin userId should return 403 | Keep | |
| DELETE to /user/{userId} should delete user and return 202 User deleted | Keep | |
| DELETE to /user/{userId} by self | Keep | |
| POST comment to comment to /discussion/{discussionId} with too long comment should return 413 Comment too long | Keep | |
| POST comment to /comment/{commentId} should return comment and 201 Comment created | Keep | |
| POST comment to /comment/{commentId} without credentials should fail | Keep | |
| POST comment to /comment/{commentId} with identical information within a short length of time should return 425 Possible duplicate comment | Replace | This currently relies on the earlier comment-create test having already inserted the same payload. Replace with a self-contained duplicate-post setup so ordering is not part of the contract. |
| GET comment to /comment/{commentId} where commentId does not exist should return 404 | Keep | |
| GET comment to /comment/{commentId} should return the comment with 200 OK | Keep | |
| PUT comment to /comment/{commentId} with no credentials should return 401 | Keep | |
| PUT comment to /comment/{commentId} with improper credentials should return 403 | Keep | |
| PUT comment to /comment/{commentId} where Id does not exist should return 404 | Keep | |
| PUT comment to /comment/{commentId} should return the edited comment with 202 Comment updated | Replace | The title says `202`, but the assertion expects `204`. Replace or rename so the test copy matches the actual contract being asserted. |
| DELETE comment to /comment/{commentId} with no credentials should return 401 | Keep | |
| DELETE comment to /comment/{commentId} with improper credentials should return 403 | Keep | |
| DELETE comment to /comment/{commentId} where Id does not exist should return 404 | Keep | |
| DELETE comment to /comment/{commentId} by an admin should delete the comment and return 202 Comment deleted | Keep | |
| DELETE comment to /comment/{commentId} by the user should delete the comment and return 202 Comment deleted | Keep | |
| POST to /topic with no credentials and policy.canPublicCreateTopic===false | Investigate | The test is gated by `!policy.canFirstVisitCreateTopic`, but the title refers to `policy.canPublicCreateTopic===false`. The name and the controlling condition do not clearly match. |
| POST to /topic with improper credentials should return 403 | Keep | |
| POST to /topic should return Discussion object and 201 Discussion created | Keep | |
| GET to /topic should return a list of topics and 200 OK | Keep | |
| GET to /topic/{topicId} should return a topic and descendent comments | Replace | The current assertion only checks the topic id and that `replies` is an array. Replace with a stronger descendant-structure assertion or rename the test to match its narrower coverage. |
| PUT topic  to /topic/{topicId} editing anything except title or isLocked should be ignored | Replace | The title overclaims. The current assertion only verifies that `dateCreated` stays unchanged, not that all other non-editable fields are ignored. |
| PUT topic to /topic/{topicId} with no credentials should return 401 | Keep | |
| PUT topic to /topic/{topicId} with improper credentials should return 403 | Keep | |
| PUT to /topic/{topicId} where Id does not exist should return 404 | Keep | |
| PUT topic with {topicId} to /topic/{topicId} should return topic and 204 Discussion updated | Keep | |
| DELETE topic to /topic/{topicId} with no credentials should return 401 | Keep | |
| DELETE topic to /topic/{topicId} with improper credentials should return 403 | Keep | |
| DELETE to /topic/{topicId} where Id does not exist should return 404 | Keep | |
| DELETE to /topic/{topicId} should delete the topic and return 202 Discussion deleted | Keep | |
| DELETE topic to /topic/{topicId} should delete descended comments | Replace | This depends on the previous topic-delete test having already removed the topic. Replace with a self-contained delete-and-verify-descendants case so the order dependency disappears. |

## src/tests/backend/SendGridNotificationService.test.ts

| Test Copy | Decision | Notes |
| --- | --- | --- |
| should set API key | Keep | Constructor-side effect is part of the service contract, and this checks the explicit override path directly. |
| should use environmental variable for API key | Keep | |
| should handle email sending failure | Replace | Important behavior, but the current mock setup only really exercises the first raced send result and does not verify the returned error body or multi-recipient failure semantics clearly. |
| should throw given empty moderator contact emails | Keep | |
| should throw given undefined moderator contact emails | Replace | The current test does not actually cover the `undefined` case; it passes `[]` again, so it duplicates the previous test instead of exercising missing env/override behavior. |
| should allow moderator contact email override when env value is missing | Keep | |
| should allow API key override when env API key is missing | Keep | |
| should send notification to moderators | Replace | Meaningful happy-path coverage, but the current test only mocks the first send result and does not assert the outbound message payloads (`to`, `from`, `subject`, `text`) for each moderator. |
| should send mulitple emails given comma-separated SIMPLE_COMMENT_MODERATOR_CONTACT_EMAIL | Keep | Useful contract check that comma-separated env configuration expands into one send call per moderator recipient. |

## src/tests/backend/api.test.ts

| Test Copy | Decision | Notes |
| --- | --- | --- |
| simple-comment-openapi3.json exists | Keep | Minimal but worthwhile guard that the OpenAPI artifact is present at the expected repo path. |
| topicGET should be defined in AbstractDbService | Replace | This row comes from `/topic get`, but the current route-normalization logic collapses it into `topicGET` instead of the actual list method `topicListGET`. Replace with an explicit route-to-service mapping table. |
| topicPOST should be defined in AbstractDbService | Replace | Meaningful parity intent, but the current check only asserts property existence on a concrete test instance. Replace with explicit route/method-to-service mapping coverage. |
| topicGET should be defined in AbstractDbService | Replace | This second `topicGET` row comes from `/topic/{topicId} get`, so the duplicate title hides the difference between list and item routes. Replace with explicit mapping coverage. |
| topicPUT should be defined in AbstractDbService | Replace | Current existence-only check gives some signal, but the file overclaims API/spec parity. Replace with stronger route/method mapping assertions. |
| topicDELETE should be defined in AbstractDbService | Replace | Current existence-only check gives some signal, but the file overclaims API/spec parity. Replace with stronger route/method mapping assertions. |
| commentPOST should be defined in AbstractDbService | Replace | Current existence-only check gives some signal, but the file overclaims API/spec parity. Replace with stronger route/method mapping assertions. |
| commentGET should be defined in AbstractDbService | Replace | Current existence-only check gives some signal, but the file overclaims API/spec parity. Replace with stronger route/method mapping assertions. |
| commentPUT should be defined in AbstractDbService | Replace | Current existence-only check gives some signal, but the file overclaims API/spec parity. Replace with stronger route/method mapping assertions. |
| commentDELETE should be defined in AbstractDbService | Replace | Current existence-only check gives some signal, but the file overclaims API/spec parity. Replace with stronger route/method mapping assertions. |
| userGET should be defined in AbstractDbService | Replace | This row comes from `/user get`, but the current normalization collapses it into `userGET` instead of the actual list method `userListGET`. Replace with explicit route-to-service mapping coverage. |
| userPOST should be defined in AbstractDbService | Replace | Meaningful parity intent, but the current check only asserts property existence on a concrete test instance. Replace with explicit route/method-to-service mapping coverage. |
| userGET should be defined in AbstractDbService | Replace | This second `userGET` row comes from `/user/{userId} get`, so the duplicate title hides the distinction between list and item routes. Replace with explicit mapping coverage. |
| userPUT should be defined in AbstractDbService | Replace | Current existence-only check gives some signal, but the file overclaims API/spec parity. Replace with stronger route/method mapping assertions. |
| userDELETE should be defined in AbstractDbService | Replace | Current existence-only check gives some signal, but the file overclaims API/spec parity. Replace with stronger route/method mapping assertions. |
| authPOST should be defined in AbstractDbService | Replace | Current existence-only check gives some signal, but the file overclaims API/spec parity. Replace with stronger route/method mapping assertions. |
| authDELETE should be defined in AbstractDbService | Replace | Current existence-only check gives some signal, but the file overclaims API/spec parity. Replace with stronger route/method mapping assertions. |
| gauthGET should be defined in AbstractDbService | Replace | Current existence-only check gives some signal, but the file overclaims API/spec parity. Replace with stronger route/method mapping assertions. |
| verifyGET should be defined in AbstractDbService | Replace | Current existence-only check gives some signal, but the file overclaims API/spec parity. Replace with stronger route/method mapping assertions. |
| non existent method on AbstractDbService should fail | Remove | Low-signal negative check: proving that a random missing property is undefined does not materially protect the OpenAPI-to-service contract. |

## src/tests/backend/crypt.test.ts

| Test Copy | Decision | Notes |
| --- | --- | --- |
| Get Expiration time | Replace | Current assertion is wall-clock-based and only checks the result stays within 10 seconds of "now." Replace with a deterministic test that verifies the requested offset and the 10-second rounding behavior in `getExpirationTime`. |
| Test auth token | Replace | This does verify the signed user claim, but the expiry assertion is coupled to the current calendar year instead of the exact token contract. Replace with a deterministic token test that passes a fixed `exp` or asserts the decoded `exp` stays within an explicit bound. |

## src/tests/backend/env-utils.test.ts

| Test Copy | Decision | Notes |
| --- | --- | --- |
| ignores blank lines and full-line comments while preserving entry order | Keep | |
| uses the first equals sign as the key/value separator | Keep | |
| fails fast when a non-comment line is missing "=" | Keep | |
| fails fast when a key is blank | Keep | |
| fails fast on duplicate keys | Keep | |
| classifies SECRET and PASSWORD keys as sensitive | Keep | Supports the bootstrap contract used by `setup-env.ts` and `setup-env.contract.test.ts`; this is meaningful behavior, not placeholder coverage. |
| builds deterministic secret replacement values | Keep | This output format is part of how test bootstrap gets predictable secret values, so keeping an explicit contract test is worthwhile. |

## src/tests/backend/mongoDb.test.ts

| Test Copy | Decision | Notes |
| --- | --- | --- |
| should insert a doc into collection | Keep | Low product-level signal, but this is a deliberate smoke test for the Jest Mongo in-memory harness that backend tests depend on. Keep it as infrastructure validation rather than treating it as business-logic coverage. |

## src/tests/backend/normalizeUrl.test.ts

| Test Copy | Decision | Notes |
| --- | --- | --- |
| should handle URLs without any of the stripped elements | Keep | |
| should strip protocol https:// | Keep | |
| should strip protocol http:// | Keep | |
| should strip authentication part of the URL | Keep | |
| should strip hash part of the URL | Keep | |
| should remove www. from the URL | Keep | |
| should remove all query parameters | Keep | |
| should remove trailing slash | Keep | |
| should remove a sole / pathname in the output | Keep | |
| should remove multiple /// | Keep | |
| should remove default directory index file from path that matches provided regex | Keep | |
| should remove default port number from the URL | Keep | |
| should not remove explicit port number from the URL | Keep | |
| should handle URLs with all of the stripped elements | Keep | Directly protects the normalization contract used by allowed-origin/referer enforcement in backend policy checks; this is focused helper coverage, not placeholder padding. |

## src/tests/backend/policyEnforcement.test.ts

| Test Copy | Decision | Notes |
| --- | --- | --- |
| should not post comment | Replace | Real signal, but the title is too vague for the actual contract. Replace with a behavior-specific title that makes the guest-account and policy condition explicit. |
| should post comment | Replace | Real signal, but the title is too vague for the actual contract. Replace with a behavior-specific title that makes the guest-account and policy condition explicit. |

## src/tests/backend/secrets.test.ts

| Test Copy | Decision | Notes |
| --- | --- | --- |
| example.env exists | Keep | Real repo contract: backend test bootstrap depends on the tracked `example.env` file being present. |
| example.env has information | Replace | Useful intent, but too vague on its own. Replace with a stronger assertion about parsed entries or required key presence rather than a generic non-empty check. |
| SIMPLE_COMMENT_MODERATOR_CONTACT_EMAIL is defined as an environmental variable | Replace | Real signal, but this row-per-key pattern is noisy. Replace with one aggregated parity check that all current `example.env` entries are injected into `process.env` after bootstrap. |
| SIMPLE_COMMENT_MODERATOR_ID is defined as an environmental variable | Replace | Real signal, but this row-per-key pattern is noisy. Replace with one aggregated parity check that all current `example.env` entries are injected into `process.env` after bootstrap. |
| SIMPLE_COMMENT_MODERATOR_PASSWORD is defined as an environmental variable | Replace | Real signal, but this row-per-key pattern is noisy. Replace with one aggregated parity check that all current `example.env` entries are injected into `process.env` after bootstrap. |
| JWT_SECRET is defined as an environmental variable | Replace | Real signal, but this row-per-key pattern is noisy. Replace with one aggregated parity check that all current `example.env` entries are injected into `process.env` after bootstrap. |
| ALLOW_ORIGIN is defined as an environmental variable | Replace | Real signal, but this row-per-key pattern is noisy. Replace with one aggregated parity check that all current `example.env` entries are injected into `process.env` after bootstrap. |
| DATABASE_ADMIN_PASSWORD is defined as an environmental variable | Replace | Real signal, but this row-per-key pattern is noisy. Replace with one aggregated parity check that all current `example.env` entries are injected into `process.env` after bootstrap. |
| DATABASE_NAME is defined as an environmental variable | Replace | Real signal, but this row-per-key pattern is noisy. Replace with one aggregated parity check that all current `example.env` entries are injected into `process.env` after bootstrap. |
| DB_CONNECTION_STRING is defined as an environmental variable | Replace | Real signal, but this row-per-key pattern is noisy. Replace with one aggregated parity check that all current `example.env` entries are injected into `process.env` after bootstrap. |
| IS_CROSS_SITE is defined as an environmental variable | Replace | Real signal, but this row-per-key pattern is noisy. Replace with one aggregated parity check that all current `example.env` entries are injected into `process.env` after bootstrap. |
| SIMPLE_COMMENT_API_URL is defined as an environmental variable | Replace | Real signal, but this row-per-key pattern is noisy. Replace with one aggregated parity check that all current `example.env` entries are injected into `process.env` after bootstrap. |
| SIMPLE_COMMENT_MODERATOR_PASSWORD is not easyToRememberHardToGuessLikeABC123 | Replace | Good intent, but this row-per-sensitive-key pattern should become one aggregated assertion that sensitive `example.env` defaults are replaced with deterministic non-default values after bootstrap. |
| JWT_SECRET is not secret-key-looks-like-aXvEQ572fvOMvQQ36K2i2PE0bwEMg9qpqBWlnPhsa5OMF1vl9NyI3TxRH0DK | Replace | Good intent, but this row-per-sensitive-key pattern should become one aggregated assertion that sensitive `example.env` defaults are replaced with deterministic non-default values after bootstrap. |
| DATABASE_ADMIN_PASSWORD is not data-base-admin-password | Replace | Good intent, but this row-per-sensitive-key pattern should become one aggregated assertion that sensitive `example.env` defaults are replaced with deterministic non-default values after bootstrap. |

## src/tests/backend/setup-env.contract.test.ts

| Test Copy | Decision | Notes |
| --- | --- | --- |
| injects missing non-sensitive keys from example env entries | Keep | Core bootstrap contract: missing non-sensitive values should be populated from parsed `example.env` entries. |
| injects deterministic non-default values for unset sensitive keys | Keep | Core bootstrap contract introduced for deterministic backend test setup. |
| replaces sensitive defaults when process.env still matches example env | Keep | Important guard against silently using default secret/password placeholders during tests. |
| does not overwrite pre-set non-default values | Keep | Important non-destructive behavior: bootstrap should not clobber already-customized env values. |
| uses the shared sensitive-key classifier during bootstrap | Replace | The title is reasonable, but the current test only calls `isSensitiveEnvKey` directly and does not actually prove bootstrap wiring uses that classifier. Replace with a bootstrap-level assertion if we want to keep this intent. |

## src/tests/backend/utilities.test.ts

| Test Copy | Decision | Notes |
| --- | --- | --- |
| should return {headers} if there is a header match | Keep | |
| should return {} if there is no header match | Keep | |
| should return { 'Access-Control-Allow-Origin': '*' } if allowed origin includes * | Keep | |
| isGuestId should pass id from generateGuestId | Keep | Small but useful cross-helper contract: generated guest ids should satisfy the guest-id predicate. |
| generates a comment ID with a given parent ID | Keep | |
| has parent id in string | Keep | |
| generates a comment ID without a parent ID | Keep | |
| generates a valid comment even with Date(0, 0) | Keep | |
| generates unique comment IDs | Keep | |
| good validateUserId | Remove | Duplicate area: `validateUserId` already has a fuller dedicated suite in `src/tests/frontend/shared-utilities.test.ts`. |
| guestId is validateUserId | Remove | Duplicate area: `validateUserId` already has a fuller dedicated suite in `src/tests/frontend/shared-utilities.test.ts`. |
| incorrect characters in validateUserId | Remove | Duplicate area: `validateUserId` already has a fuller dedicated suite in `src/tests/frontend/shared-utilities.test.ts`. |
| not enough characters in validateUserId | Remove | Duplicate area: `validateUserId` already has a fuller dedicated suite in `src/tests/frontend/shared-utilities.test.ts`. |
| too many characters in validateUserId | Remove | Duplicate area: `validateUserId` already has a fuller dedicated suite in `src/tests/frontend/shared-utilities.test.ts`. |
| bad email should fail | Replace | Useful validation intent, but the test uses randomly generated input, which adds noise without improving coverage. Replace with fixed representative addresses. |
| good email should pass | Replace | Useful validation intent, but the test uses randomly generated input, which adds noise without improving coverage. Replace with fixed representative addresses. |
| should handle URLs with hash, query parameters, and directory index | Remove | This indirectly rechecks `normalizeUrl` behavior through `isAllowedReferer`, but `normalizeUrl` now has its own dedicated focused suite. |
| should handle URLs without any of the stripped elements | Remove | This indirectly rechecks `normalizeUrl` behavior through `isAllowedReferer`, but `normalizeUrl` now has its own dedicated focused suite. |
| should parse correctly | Keep | |
| should decode URI components | Keep | |
| should handle empty strings | Keep | |
| should handle keys without values | Keep | |
| should handle values without keys | Keep | |
| should throw an error for malformed URI sequences | Keep | |
| should add headers to a response without a body | Keep | |
| should add headers and stringify a JSON body | Keep | |
| should add headers and leave a string body as is | Keep | |
| isApiError should detect API-style error objects | Keep | |
| toApiError should return fallback for unknown errors | Keep | |
| toApiError should keep API-style errors | Keep | |
| toErrorBody should normalize unknown values to strings | Keep | |

## src/tests/frontend/blockies.test.ts

| Test Copy | Decision | Notes |
| --- | --- | --- |
| should return a different number for different seeds | Keep | Basic seeded-generator sanity check for a helper that drives frontend avatar rendering. |
| 1 should return identical results | Replace | Real deterministic intent, but six numbered rows are repetitive. Replace this group with one assertion over a fixed sequence length. |
| 2 should return identical results | Replace | Real deterministic intent, but six numbered rows are repetitive. Replace this group with one assertion over a fixed sequence length. |
| 3 should return identical results | Replace | Real deterministic intent, but six numbered rows are repetitive. Replace this group with one assertion over a fixed sequence length. |
| 4 should return identical results | Replace | Real deterministic intent, but six numbered rows are repetitive. Replace this group with one assertion over a fixed sequence length. |
| 5 should return identical results | Replace | Real deterministic intent, but six numbered rows are repetitive. Replace this group with one assertion over a fixed sequence length. |
| 6 should return identical results | Replace | Real deterministic intent, but six numbered rows are repetitive. Replace this group with one assertion over a fixed sequence length. |
| 1 should return different results | Replace | Real distinct-seed intent, but three numbered rows are repetitive. Replace this group with one assertion over a fixed sequence length. |
| 2 should return different results | Replace | Real distinct-seed intent, but three numbered rows are repetitive. Replace this group with one assertion over a fixed sequence length. |
| 3 should return different results | Replace | Real distinct-seed intent, but three numbered rows are repetitive. Replace this group with one assertion over a fixed sequence length. |
| should return deterministic results | Keep | Deterministic color generation matters because blockie output is seed-based UI identity. |
| should return a string | Keep | |
| should return a different color for different seeds | Keep | |
| should return deterministic results | Keep | Deterministic image data matters because frontend avatar rendering depends on stable seeded output. |
| should return an array | Keep | |
| should return a different array for different seeds | Keep | |

## src/tests/frontend/discussion.xstate.test.ts

| Test Copy | Decision | Notes |
| --- | --- | --- |
| init state should be 'idle' | Replace | The title is stale: the assertion expects `loading`, which matches the machine's `idle -> always -> loading` behavior. Replace with a title that matches the effective initial state contract. |
| loading state should allow only actions SUCCESS or ERROR | Replace | Real signal, but this is tightly coupled to the machine definition shape (`definition.states...on`) rather than observable workflow behavior. |
| creating state should allow only actions SUCCESS or ERROR | Replace | Real signal, but this is tightly coupled to the machine definition shape (`definition.states...on`) rather than observable workflow behavior. |
| 'created' state should always transition to 'loading' | Keep | This protects a meaningful workflow shortcut in the machine, where successful creation immediately rolls into loading. |
| loaded state should allow only action LOAD | Replace | Real signal, but this is tightly coupled to the machine definition shape (`definition.states...on`) rather than observable workflow behavior. |
| error state should allow only actions RETRY or CREATE | Replace | Real signal, but this is tightly coupled to the machine definition shape (`definition.states...on`) rather than observable workflow behavior. |
| loading with SUCCESS should transition to state `loaded` | Keep | |
| loading with ERROR should transition to state `error` | Keep | |
| creating with SUCCESS should transition to state `created` | Replace | The title is stale: the assertion expects `loading`, not `created`, because `created` immediately self-advances. Replace with a title that matches the asserted transition behavior. |
| creating with ERROR should transition to state `error` | Keep | |
| loaded with LOAD should transition to state `loading` | Keep | |
| error with RETRY should transition to state `loading` | Keep | |
| error with CREATE should transition to state `creating` | Keep | |

## src/tests/frontend/frontend-utilities.test.ts

| Test Copy | Decision | Notes |
| --- | --- | --- |
| should thread comments correctly | Keep | Core behavior check for a utility used by `DiscussionDisplay.svelte` to build nested reply trees. |
| should thread mockDiscussion.json in under 0.1s | Replace | Useful performance intent, but fixed wall-clock thresholds are fragile across machines and CI environments. Replace with a less brittle performance signal or move to non-blocking profiling. |
| should thread flat array of 2000 comments in under 1s | Replace | Useful performance intent, but fixed wall-clock thresholds are fragile across machines and CI environments. Replace with a less brittle performance signal or move to non-blocking profiling. |
| calls as expected | Keep | |
| does not call immediately | Replace | Real debounce intent, but this relies on elapsed wall-clock timing instead of using fake timers or more deterministic scheduling assertions. |
| never calls callback if continuously called | Replace | Real debounce intent, but this relies on real timers and interval timing, which makes it more fragile than it needs to be. |
| calls after its wait time | Replace | Real debounce intent, but this relies on real timers and elapsed wall-clock checks instead of fake timers. |
| calls once only after debounce ends | Replace | Real debounce intent, but this relies on real timers and interval timing, which makes it more fragile than it needs to be. |
| should handle undefined date | Keep | |
| should handle invalid date | Keep | |

## src/tests/frontend/hello-world.test.ts

| Test Copy | Decision | Notes |
| --- | --- | --- |
| adds 1 + 2 to equal 3 | Remove | Pure placeholder test: it only exercises a local `sum` function declared inside the test file and does not protect any frontend product behavior, shared utility, or contract. |

## src/tests/frontend/login.xstate.test.ts

| Test Copy | Decision | Notes |
| --- | --- | --- |
| init state should be 'verifying' | Keep | Effective initial-state contract matters because `Login.svelte` immediately begins auth verification on mount. |
| loggingIn state should allow only actions SUCCESS or ERROR | Replace | Real signal, but this is tightly coupled to the machine definition shape (`definition.states...on`) rather than observable workflow behavior. |
| signingUp state should allow only actions SUCCESS or ERROR | Replace | Real signal, but this is tightly coupled to the machine definition shape (`definition.states...on`) rather than observable workflow behavior. |
| loggedIn state should allow only action LOGOUT | Replace | Real signal, but this is tightly coupled to the machine definition shape (`definition.states...on`) rather than observable workflow behavior. |
| loggingOut state should allow only actions SUCCESS, CONFIRM, CANCEL, SIGNUP or ERROR | Replace | Real signal, but this is tightly coupled to the machine definition shape (`definition.states...on`) rather than observable workflow behavior. |
| loggedOut state should allow only actions 'LOGIN', 'SIGNUP', 'GUEST', 'ERROR' | Replace | Real signal, but this is tightly coupled to the machine definition shape (`definition.states...on`) rather than observable workflow behavior. |
| error state should allow only actions '*' and LOGOUT | Replace | Real signal, but this is tightly coupled to the machine definition shape (`definition.states...on`) rather than observable workflow behavior. |
| verifying state should allow only actions SUCCESS, ERROR or FIRST_VISIT | Replace | Real signal, but this is tightly coupled to the machine definition shape (`definition.states...on`) rather than observable workflow behavior. |
| verifying with SUCCESS should transition to state `loggedIn` | Keep | |
| verifying with ERROR should transition to state `error` | Keep | |
| loggingIn with SUCCESS should transition to state `verifying` | Keep | |
| signingUp with SUCCESS should transition to state `loggingIn` | Keep | Effective workflow is correct because `signedUp` immediately self-advances into `loggingIn`. |
| loggingIn with ERROR should transition to state `error` | Keep | |
| signingUp with ERROR should transition to state `error` | Keep | |
| loggedIn with LOGOUT should transition to state `loggingOut` | Keep | |

## src/tests/frontend/shared-utilities.test.ts

| Test Copy | Decision | Notes |
| --- | --- | --- |
| returns valid result when all validations are valid | Keep | |
| returns invalid result with combined reason when all validations are invalid | Keep | |
| returns invalid result with combined reason when some validations are invalid | Keep | |
| returns valid result when there are no validations | Keep | Good edge-case contract for `joinValidations`, since the reducer intentionally defaults to a valid result. |
| should return valid for a valid user id | Keep | |
| should return invalid for an null user id | Keep | Runtime guard is intentional and worth testing even though the helper is typed for strings. |
| should return invalid for an undefined user id | Keep | Runtime guard is intentional and worth testing even though the helper is typed for strings. |
| should return invalid for a non-string user id | Keep | Runtime guard is intentional and worth testing even though the helper is typed for strings. |
| should return invalid for a user id shorter than 5 characters | Keep | |
| should return invalid for a user id longer than 36 characters | Keep | |
| should return invalid for a user id with invalid characters | Keep | |
| should return invalid for a user id with multiple issues | Keep | This is the strongest case in the suite because it confirms `joinValidations` and `validateUserId` interact correctly to preserve multiple failure reasons. |

## src/tests/frontend/svelte-stores.test.ts

| Test Copy | Decision | Notes |
| --- | --- | --- |
| should initialize with the correct default state | Investigate | The store is used by live components, but the exact sentinel value `{ name: "init" }` appears to be an implementation detail rather than a user-facing contract. Decide whether we care about an inert initial event generically or this exact payload specifically. |
| should dispatch and update the state correctly | Keep | `dispatchableStore.dispatch()` is real app wiring used by components like `Login.svelte` and `CommentInput.svelte`, so keeping a direct contract check on the dispatched event payload is worthwhile. |
